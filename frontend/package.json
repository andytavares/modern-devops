{
  "name": "shop-canary-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --test src/*.test.ts"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "@types/node": "26.2.0"
  }
}
