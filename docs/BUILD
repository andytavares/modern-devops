# The docs are inputs to two checks in `checks/`: the assembled single-file
# edition, and the listing-drift check. Neither is reachable by import, so
# nothing infers them.
files(name="docs", sources=["*.md"])
